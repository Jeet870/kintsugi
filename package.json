{
  "name": "kintsugi-sanctuary",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "postinstall": "npm --prefix web install",
    "build": "node -e \"require('fs').rmSync('dist', {recursive: true, force: true})\" && npm --prefix web run build && node -e \"require('fs').cpSync('web/dist', 'dist', {recursive: true})\"",
    "start": "npm --prefix web run preview"
  }
}
